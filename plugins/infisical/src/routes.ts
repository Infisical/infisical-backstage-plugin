/**
 * Route definitions for the Infisical plugin
 */

import { createRouteRef } from '@backstage/core-plugin-api';

/**
 * Root route reference for the Infisical plugin
 */
export const rootRouteRef = createRouteRef({
  id: 'infisical',
});
